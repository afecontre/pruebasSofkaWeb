/**
 * Este paquete contiene las clases de modelo que se utilizan en el proyecto de Serenity BDD para representar los datos y objetos que se utilizan en las pruebas automatizadas.
 * Los modelos se usan para representar objetos como usuarios, productos, pedidos, etc. Estos modelos se utilizan en combinación con las tareas (tasks) y las preguntas (questions) para definir y ejecutar las pruebas automatizadas.
 * <p>
 * Las clases de modelo incluyen campos que representan los datos asociados con el objeto, como el nombre del usuario, la dirección de correo electrónico, el precio del producto, etc. También pueden incluir métodos que se utilizan para interactuar con los datos, como los métodos para establecer y obtener los valores de los campos.
 * </p>
 * <p>
 * Es importante que los modelos se mantengan actualizados y sean precisos para garantizar que las pruebas automatizadas se ejecuten correctamente. Los modelos también deben ser coherentes con la interfaz de usuario y la lógica de negocio del sistema que se está probando.
 * </p>
 *
 */
package co.com.certification.testing.models;